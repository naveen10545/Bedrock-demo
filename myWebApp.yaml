# mywebapp services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: mywebapp-workorder1
  labels:
    app: mywebapp-workorder1
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: http
  selector:
    app: mywebapp-workorder1
---
apiVersion: extensions/v1beta1 #TODO: Migrate to apps/v1
kind: Deployment
metadata:
  name: mywebapp-workorder1-v1
spec:
  replicas: 3
  minReadySeconds: 10 # Wait 2 seconds after each new pod comes up before marked as "ready"
  strategy:
    type: RollingUpdate # describe how we do rolling updates
    rollingUpdate:
      maxUnavailable: 1 # When updating take one pod down at a time
      maxSurge: 1 # When updating never have more than one extra pod. If replicas = 2 then never 3 pods when updating
  template:
    metadata:
      labels:
        app: mywebapp-workorder1
        version: v1
    spec:
      containers:
        - name: mywebapp-workorder1
          image: acrcopopreprod.azurecr.io/copo-workorder-service-dev:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080

---

  # mywebapp services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: mywebapp-common
  labels:
    app: mywebapp-common
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: http
  selector:
    app: mywebapp-common
---
apiVersion: extensions/v1beta1 #TODO: Migrate to apps/v1
kind: Deployment
metadata:
  name: mywebapp-common-v1
spec:
  replicas: 3
  minReadySeconds: 10 # Wait 2 seconds after each new pod comes up before marked as "ready"
  strategy:
    type: RollingUpdate # describe how we do rolling updates
    rollingUpdate:
      maxUnavailable: 1 # When updating take one pod down at a time
      maxSurge: 1 # When updating never have more than one extra pod. If replicas = 2 then never 3 pods when updating
  template:
    metadata:
      labels:
        app: mywebapp-common
        version: v1
    spec:
      containers:
        - name: mywebapp-common
          image: acrcopopreprod.azurecr.io/copo-common-service-dev:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
---

# mywebapp services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: mywebapp-org
  labels:
    app: mywebapp-org
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: http
  selector:
    app: mywebapp-org
---
apiVersion: extensions/v1beta1 #TODO: Migrate to apps/v1
kind: Deployment
metadata:
  name: mywebapp-org-v1
spec:
  replicas: 3
  minReadySeconds: 10 # Wait 2 seconds after each new pod comes up before marked as "ready"
  strategy:
    type: RollingUpdate # describe how we do rolling updates
    rollingUpdate:
      maxUnavailable: 1 # When updating take one pod down at a time
      maxSurge: 1 # When updating never have more than one extra pod. If replicas = 2 then never 3 pods when updating
  template:
    metadata:
      labels:
        app: mywebapp-org
        version: v1
    spec:
      containers:
        - name: mywebapp-org
          image: acrcopopreprod.azurecr.io/copo-organization-service-dev:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
        
---

  # mywebapp services
##################################################################################################
apiVersion: v1
kind: Service
metadata:
  name: mywebapp-ui
  labels:
    app: mywebapp-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      name: http
  selector:
    app: mywebapp-ui
---
apiVersion: extensions/v1beta1 #TODO: Migrate to apps/v1
kind: Deployment
metadata:
  name: mywebapp-ui-v1
spec:
  replicas: 3
  minReadySeconds: 10 # Wait 2 seconds after each new pod comes up before marked as "ready"
  strategy:
    type: RollingUpdate # describe how we do rolling updates
    rollingUpdate:
      maxUnavailable: 1 # When updating take one pod down at a time
      maxSurge: 1 # When updating never have more than one extra pod. If replicas = 2 then never 3 pods when updating
  template:
    metadata:
      labels:
        app: mywebapp-ui
        version: v1
    spec:
      containers:
        - name: mywebapp-ui
          image: acrcopopreprod.azurecr.io/copo-ui-service-dev:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
